apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.provisioning.dashboardFiles.configMapName }}
  namespace: {{ .Values.namespace }}
data:
  dashboard.yml: |
{{ .Files.Get "provisioning/dashboards/dashboard.yml" | indent 4 }}