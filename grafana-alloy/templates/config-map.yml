{{- $file := .Values.config.filePathSource }}

apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.appName }}
  namespace: {{ .Values.namespace }}
data:
  config.alloy: |
{{ .Files.Get $file | indent 4 }}